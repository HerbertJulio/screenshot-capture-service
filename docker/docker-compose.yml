services:
  scs:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - HOST=0.0.0.0
      - NODE_ENV=production
      - API_KEY=${API_KEY}
      - WEBHOOK_SECRET=${WEBHOOK_SECRET}
      - S3_ENDPOINT=${S3_ENDPOINT:-https://s3.us-east-005.azionstorage.net}
      - S3_REGION=${S3_REGION:-us-east-005}
      - S3_BUCKET=${S3_BUCKET:-screenshot-captures}
      - S3_ACCESS_KEY_ID=${S3_ACCESS_KEY_ID}
      - S3_SECRET_ACCESS_KEY=${S3_SECRET_ACCESS_KEY}
      - CDN_BASE_URL=${CDN_BASE_URL:-https://your-domain.map.azionedge.net}
      - DB_PATH=/app/data/scs.db
    volumes:
      - scs-data:/app/data
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: "1.0"

  # Local S3 for development
  localstack:
    image: localstack/localstack:3
    ports:
      - "4566:4566"
    environment:
      - SERVICES=s3
      - DEFAULT_REGION=us-east-1
    volumes:
      - localstack-data:/var/lib/localstack

volumes:
  scs-data:
  localstack-data:
